import numpy as np

def create_dist_array(h,k,shape):
    """creates an array where each element is euclidian distance from point (h,k)
    
    Args:
        h: x-axis value
        k: y-axis value
        
    Returns:
        distance matrix
    """
    #return np.sqrt(xv**2 + yv**2)
    return